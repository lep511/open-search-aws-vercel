[project]
name = "pdf-ingestion"
version = "1.0.0"
description = "Extract text from PDFs and upload to OpenSearch via API"
requires-python = ">=3.11"
dependencies = [
    "pdfmux[marker]==1.7.0",
    "httpx>=0.27.0",
]

[project.scripts]
ingest = "ingest:main"
